use_aws_profile(){
  profile_name=$1

  export AWS_ASSUME_ROLE_TTL=1h
  export $(aws-vault exec $profile_name --prompt=osascript -- env | grep AWS_ | grep -v AWS_VAULT)
}

use_aws_profile_with_sso(){
  sso_session=$1

  aws sso login --sso-session ${sso_session}
}
